# Not-in operator

Not-in operator

## Usage

``` r
x %notin% table
```

## Arguments

- x:

  Values to check

- table:

  Values to check against

## Value

A logical vector, `TRUE` where an element of `x` is not present in
`table`.
